class loja:
    def __init__(self, cliente, funcionarios):
        self.funionarios = funcionarios
        self.cliente = cliente

        def qtdClientes(self):
            quantidade = self.Clientes
            print(quantidade)

        def qtdFuncionario(self):
            quantidadefun = self.Funcionarios
            print(quantidadefun)

        objeto = loja(1000, 15)
        print(object.quantidadeFun())
        print(object.quantidade())
